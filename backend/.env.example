# BuildMatch AI backend secrets — REAL VALUES ONLY on the server.
# Set these with the Supabase CLI (never commit real keys):
#
#   supabase secrets set GEMINI_API_KEY=your_real_gemini_key
#   supabase secrets set GEMINI_MODEL=gemini-3.6-flash
#
# Get a real key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
