# BuildMatch frontend env — copy to .env.local and fill in.
# NEVER put secret keys here: all AI keys live in backend Supabase secrets
# (GEMINI_API_KEY / GEMINI_MODEL — see backend/.env.example).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
